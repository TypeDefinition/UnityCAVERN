using UnityEngine;

namespace Spelunx {
    /// Placeholder class to ensure that Doxygen is working.
    public class DoxygenPlaceholder : MonoBehaviour {
        /**
         * \brief Function that adds one to an integer.
         * \params x The input integer.
         * \return Returns x + 1.
         */
        public int AddOne(int x) {
            return x + 1;
        }

        /// Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start() {

        }

        /// Update is called once per frame
        void Update() {

        }
    }
}